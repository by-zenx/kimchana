'use client';

export function Grid() {
  // This component serves as a container for the grid layout
  // The actual grid rendering is handled in GameBoard component
  // This is kept for architectural consistency
  return null;
}
